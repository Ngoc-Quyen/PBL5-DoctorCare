const path = require('path');

module.exports = {
    config: path.resolve('./src/config', 'config.json'),
    'migrations-path': path.resolve('./src', 'migrations'), // tạo table cho database
    'models-path': path.resolve('./src', 'models'),
    'seeders-path': path.resolve('./src', 'seeders'), // tạo dữ liệu fake
};

<!DOCTYPE html>
<html lang="en">

<head>
    <title>Doctors Care</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <!-- Bootstrap 4.3, Jquery 3.3, font-awesome 4.7, slick-slider-->
    <script src="/components/jquery/dist/jquery.min.js"></script>
    <link rel="stylesheet" href="/components/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/auth.css">
    <link rel="stylesheet" href="/components/font-awesome/css/font-awesome.min.css">
    <script src="/components/bootstrap/js/bootstrap.min.js"></script>
</head>

<body>

    <div class="login">
        <div class="container lg-body">
            <div class="container d-flex flex-row">
                <div class="col-6 lg-image-background">
                </div>
                <div class="col-6 login-form">
                    <span class="text-login">Đăng nhập vào Doctor Care</span>
                    <% if(error !="" ){ %>
                        <div class="alert alert-danger">
                            <%= error %>
                        </div>
                        <% } %>
                            <form method="POST" action="/login">
                                <div class="form-group">
                                    <label for="email">Địa chỉ email:</label>
                                    <input type="email" class="form-control" id="email" name="email" placeholder="Email">
                                </div>
                                <div class="form-group">
                                    <label for="password">Mật khẩu:</label>
                                    <input type="password" class="form-control" id="password" name="password" placeholder="Mật khẩu">
                                </div>
                                <div class="forgot-password">
                                    <a href="#" data-toggle="modal" data-target="#forgotPasswordModal">Quên mật khẩu
                                        ???</a>
                                </div>
                                <button type="submit" class="btn btn-primary btn-login ">ĐĂNG NHẬP</button>
                            </form>
                            <br>
                            <hr>
                            <a class="lg-register" href="/"><i class="fa fa-arrow-left" aria-hidden="true"></i> Về lại
                                trang chủ </a> <br />
                            <a title="Click to view my Facebook Page" class="lg-register" target="_blank" href="https://www.facebook.com/profile.php?id=100011010698678"><i style="color: orange"
                                    class="fa fa-comments" aria-hidden="true"></i> Truy cập vào FANPAGE của chúng
                                tôi</a>

                </div>
            </div>
        </div>
    </div>

    <!-- Modal -->
    <form class="modal fade" id="forgotPasswordModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true" action="/reset-password" method="GET">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">Quên mật khẩu ??? Vui lòng điền địa chỉ email để tìm tài khoản của bạn </h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <div>
                        <label for="input-forgot-password">Địa chỉ email:</label>
                        <input id="input-forgot-password" type="email" class="form-control" placeholder="Email..." name="emailResetPassword">
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Hủy</button>
                    <button type="submit" class="btn btn-primary" id="forgotPasswordOkButton">OK</button>
                </div>
            </div>
        </div>
    </form>

    <!-- Đoạn mã JavaScript -->
    <script>
        // Lấy thông tin lỗi từ server (nếu có)
        const errorMessage = '<%= typeof message !== "undefined" ? message : "" %>';

        // Hiển thị thông báo lỗi (nếu có)
        if (errorMessage) {
            document.getElementById('error-message').innerText = errorMessage;
        }
    </script>
</body>

</html>